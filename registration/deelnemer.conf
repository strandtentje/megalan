{
	instances = {
		langfout = Template(templatefile = f"-templates/1-100.html");
		kortfout = Template(templatefile = f"-templates/1-10.html");
		langopt = Template(templatefile = f"-templates/0-100.html");
		
		acquire = MySqlQuerier(query = f"-queries/GetSessieDeelnemer.sql", params = ["sessieid"], credset = "megalan") {
			log = true;
			none_branch = MySqlQuerier(query = f"-queries/SetSessieDeelnemer.sql", params = ["sessieid"], credset = "megalan") {
				log = true;
				single_branch = Return(branchname = "acquired");
			};
			single_branch = Return(branchname = "acquired");
		};
	};
}