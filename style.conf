{
	instances = {
		css = Template(templatefile = f"-styles/style.css") {
			background_branch = Write(format = "black");
			foreground_branch = Write(format = "white");
			shade_branch = Write(format = "#0f0");			
			accent_branch = Write(format = "#0ff");
		};
	};
}